import React from "react";
import { CreateChat } from "../components";

export const CreateChatPage: React.FC = () => {
  return (
    <section className="w-full min-h-screen flex items-center justify-center">
      <CreateChat />
    </section>
  );
};
